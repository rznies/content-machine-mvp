## Agent skills

### Issue tracker

GitHub Issues is used as the issue tracker for this repository. See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage labels (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix) are used. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context project layout is used (one CONTEXT.md and docs/adr/ at the root). See `docs/agents/domain.md`.
